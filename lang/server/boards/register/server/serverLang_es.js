let data = require('../data')

/*
  *[D] createUser
  *[i] No tocar las etiquetas <b> o <p>, solo tocar el contenido que se encuentra dentro de esas etiquetas
*/
data.registerServer.createUser = '<b> Servidor </b> <p> Algunos fragmentos de datos han fallado en nuestros servidores </p>'

module.exports = data
