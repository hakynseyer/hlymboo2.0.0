let data = require('../data')

module.exports = () => {
  /*
    *[D] regActivationServer
    *[i] Elemento Html
  */
  data.regActivationValidator.regActivationServer = '<b> Servidor </b> <p> Algunos fragmentos de datos han fallado en nuestros servidores </p>'

  return data
}
